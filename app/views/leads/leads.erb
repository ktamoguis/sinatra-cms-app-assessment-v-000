<h1>Welcome, <%= @user.name %> </h1>

<h2>Region: <%= @user.region.name %></h2>

<p><a href="/leads/new">Create New Lead</a> or <a href="/logout">Log Out</a> to continue.
<br><br>

<p>OR</p>
<form action="/leads/delete" method="post">
  <input id="hidden" type="hidden" name="_method" value="delete">
  <label>Input ID no of lead to delete:</label>
  <input type="text" name="lead_id">
  <input type="submit" name="submit">
</form>
<br><br>
<h2>Your Leads</h2>
<table>
  <tr>
    <th>ID no</th>
    <th>Lead Name</th>
    <th>Status</th>
  </tr>
  <% @user.leads.each do |lead| %>
  <tr>
      <td><%= lead.id %></td>
      <td><%= lead.name %></td>
      <td><%= lead.status %></td>
  </tr>
  <% end %>
</table>
